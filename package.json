{
  "name": "autofox",
  "version": "0.2.0",
  "description": "AI Desktop Agent — a self-contained desktop app for AI-powered task automation",
  "module": "server/index.ts",
  "type": "module",
  "private": false,
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "start": "bun run server/index.ts",
    "test": "bun test",
    "typecheck": "bun run tsc --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
